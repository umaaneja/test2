php artisan make:model Models/Page


mkdir -p app/Models
touch app/Models/Page.php



<?php

namespace App\Models;

use Illuminate\Database\Eloquent\Model;
use App\Models\Paragraph;

class Page extends Model
{
    protected $fillable = ['title', 'slug']; // add more fields as needed

    public function paragraphs()
    {
        return $this->hasMany(Paragraph::class);
    }

    public function paragraphsByRegion(string $region)
    {
        return $this->paragraphs()
            ->where('region', $region)
            ->orderBy('position')
            ->get();
    }

    public function availableRegions(): array
    {
        return $this->paragraphs()
            ->select('region')
            ->distinct()
            ->pluck('region')
            ->toArray();
    }
}
