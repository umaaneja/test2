use App\Http\Controllers\PageController;

Route::get('/pages/{slug}', [PageController::class, 'show'])->name('pages.show')

php artisan make:controller PageController
Then in app/Http/Controllers/PageController.php:

php
Copy
Edit
namespace App\Http\Controllers;

use App\Models\Page;
use Illuminate\Http\Request;

class PageController extends Controller
{
    public function show($slug)
    {
        // Just get the page; let the paragraph plugin load the paragraphs internally
        $page = Page::where('slug', $slug)->firstOrFail();

        return view('pages.show', compact('page'));
    }
}


. Create the Blade View
In resources/views/pages/show.blade.php:

blade
Copy
Edit
@extends('layouts.app')

@section('content')
    <h1>{{ $page->title }}</h1>

    {{-- Render main content paragraphs --}}
    <x-paragraph-region :page="$page" region="content" />

    {{-- Render sidebar if needed --}}
    <x-paragraph-region :page="$page" region="sidebar" />
@endsection
