use App\Models\Page;
use App\Models\GalleryBlock; // adjust namespace based on your app

// Step 1: Create the gallery block with images
$galleryBlock = GalleryBlock::create([
    'title' => 'Our Summer Trip',
    'images' => [
        'gallery/image1.jpg',
        'gallery/image2.jpg',
        'gallery/image3.jpg'
    ]
]);

// Step 2: Attach it to a specific page
$page = Page::find(1); // Replace with actual page ID

$page->paragraphs()->create([
    'paragraphable_type' => get_class($galleryBlock),
    'paragraphable_id' => $galleryBlock->id,
    'region' => 'sidebar', // or 'content'
    'position' => 1
]);



<div class="content">
    @foreach($page->paragraphsByRegion('content') as $para)
        @includeIf('paragraphs.' . strtolower(class_basename($para->paragraphable_type)), ['data' => $para->paragraphable])
    @endforeach
</div>
